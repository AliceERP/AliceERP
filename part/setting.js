'use strict'
const db = require('@zserver/database') // access database

module.exports = {
    erpSiteFolderName: async () => {
        // check old folder name

        // check if same

        // check if existing

        // if new, create new folder name

        // update everything in system that links to this
    }
}